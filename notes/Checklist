TO DO:
Revisar archivos viejos (De los primeros que hice) a ver si se pueden agregar mónadas (State, Maybe, Either, etc)
BoardGeneration: Considerar hacer una función updateBoard
                 Si quiero generar un board con bordes y falla por obstáculos mal puestos, las coordenadas que me devuelve no me sirven porque están offseteadas
Comentar todo el código

