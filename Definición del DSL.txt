-- Definición de Party
-- Sólo sería dar nombres y cantidad. Podría permitir que se pase cómo argumento cuántos están.



-- Definición del mapa
-- Se me ocurre permitir height * width, random con algún valor o dar el contorno.



-- Definición del objetivo de la pelea
-- Derrotar a todos los enemigos, sobrevivir cierta cantidad de tiempo, llegar a un punto.



-- Definición de los enemigos
-- Stat blocks en general, esta parte es un bajón.



-- Definición de la IA de cada enemigo

-- Basic Actions. Acciones que toman un turno en total.
BA := ...

-- Behaviours
BV :=   BA
        BV1 -> BV2                   -- Secuenciación de comportamientos.
        Choice (BVList)              -- Elección al azar entre una lista de comportamientos.
        Util (WBAList)               -- Evalúa la utilidad de una lista de acciones básicas,
                                     -- cada una con pesos, y elige la más conveniente.
        Repeat (Condition -> BV)        -- Repite un comportamiento mientras se cumpla una condición.
        Condition ? BV

-- Permitir Interleaving de acciones?